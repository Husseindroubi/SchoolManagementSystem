@model ModelsLayer.Subject

<br />
<br />
<br />
<br />
<br />

@if(User.IsInRole("Admin"))
{
<h1 class="text-white">Delete Subject</h1>
<div>
    <h3 class="text-white">Are you sure you want to delete this Subject?</h3>
    <dl class="row text-white">
            <dt class="col-sm-2">Name</dt>
        <dd class="col-sm-10">@Model.Name</dd>
            <dt class="col-sm-2">Descriptiondt>
        <dd class="col-sm-10">@Model.Description</dd>
    </dl>
    <form asp-controller="Subject" asp-action="Delete" method="post">
        <input type="hidden" asp-for="SubjectId" />
        <button type="submit" class="btn btn-danger">Delete</button>
            <a asp-action="Index" class="btn btn-secondary">Back to List</a>
    </form>
</div>
}
else
{
    <a asp-action="Index" class="btn btn-secondary">Back to List</a>
}
